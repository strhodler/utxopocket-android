---
id: branch-and-bound
title: Branch-and-bound (coin selection)
summary: Searches input combinations to hit a target exactly or near-optimally; fewer inputs, potentially slower.
related: [utxo-selection-heuristics]
keywords: [coin selection]
---

Branch-and-bound explores combinations of inputs to reach an exact target or a close fit. It often yields fewer inputs and avoids change but can be slower. See the selection heuristics article for practical constraints.

