<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <!--
      Auto Backup is disabled in AndroidManifest.xml (android:allowBackup="false").
      Keep extraction sections empty to make the no-backup intent explicit.
    -->
    <cloud-backup />
    <device-transfer />
</data-extraction-rules>
